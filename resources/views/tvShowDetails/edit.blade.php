@extends('tvShowDetails.layout')
   
@section('content')
    <div class="row">
        <div class="col-lg-12 margin-tb">
            <div class="pull-left">
                <h2>Edit Quote</h2>
            </div>
            <div class="pull-right">
                <a class="btn btn-primary" href="{{ route('tvShowDetails.index') }}"> Back</a>
            </div>
        </div>
    </div>
   
    @if ($errors->any())
        <div class="alert alert-danger">
            <strong>Warning!</strong> Please check input field code<br><br>
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
  
    <form action="{{ route('tvShowDetails.update',$showDetail->id) }}" method="POST">
        @csrf
        @method('PUT')
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Season:</strong>
                    <input type="text" name="season" value="{{ $showDetail->season }}" class="form-control" placeholder="season">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Episode:</strong>
                    <input type="text" name="episode" value="{{ $showDetail->episode }}" class="form-control" placeholder="episode">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Quote:</strong>
                    <textarea class="form-control" style="height:150px" name="quote" placeholder="Quote">{{ $showDetail->quote }}</textarea>
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 text-center">
              <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </div>
      </form>
@endsection